$(function() {
  $('.header-logo').click(function() {
    $('html, body').animate({
      'scrollTop': 0
    },500)
  });
  $(window).scroll(function (){
    $("#sample1").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 270){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $("#sample2").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 270){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $(".check").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 330){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $(".ppp").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 270){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $(".ppp1").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 270){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $(".ppp2").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 170){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $("#sample3").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 270){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $("#sample4").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 270){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $("#sample5").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 270){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $("#sample6").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 400){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $("#sample7").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 400){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $("#sample8").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 400){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $("#sample9").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 300){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $("#sample10").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 300){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $(".p2p").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 100){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $("#sample11").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 300){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $("#sample12").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 220){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $("#sample13").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 360){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $(".fail-wrapper").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 300){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $(".heading1").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 270){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $("#sample14").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 270){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $("#sample15").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 400){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $("#sample16").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 400){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
    $("#sample17").each(function(){
      var imgPos = $(this).offset().top;
      var scroll = $(window).scrollTop();
      var windowHeight = $(window).height();
      if (scroll > imgPos - windowHeight + 400){
        $(this).addClass("fade_on");
      } else {
        $(this).removeClass("fade_on");
      }
    });
  });
});
